# Problem 008 - Largest product in a series

## Approach
Iterated through the 1000-digit number and calculated the product of every contiguous sequence of 13 digits.

## Complexity
- **Time:** $O(N)$ where N is the number of digits.
- **Space:** $O(N)$ to store the string.
